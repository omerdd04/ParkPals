# 🔌 חיבור השרת (Supabase) — מדריך צעד-אחר-צעד

האפליקציה עובדת גם בלי שרת (מצב דמו, נתונים מקומיים). ברגע שתחבר את שני המפתחות
למטה, היא עוברת אוטומטית ל**שרת חי**: חשבונות אמיתיים, כניסה באימייל, ובהמשך
נוכחות/חברים/צ'אט בזמן אמת.

צריך לעשות את זה **פעם אחת**. ~10 דקות.

---

## שלב 1 — להריץ את הסכימה של בסיס הנתונים
1. היכנס ל-Supabase → הפרויקט שלך.
2. בתפריט משמאל: **SQL Editor** → **New query**.
3. פתח את הקובץ `supabase/schema.sql` מהפרויקט הזה, **העתק את כל התוכן**, הדבק בעורך.
4. לחץ **Run** (ירוק, למטה מימין). אמור להופיע "Success". אפשר להריץ שוב בלי חשש.

זה יוצר את הטבלאות (פרופילים, נוכחות, חברים, הודעות, תלונות), את חוקי האבטחה (RLS),
ומפעיל עדכונים בזמן אמת.

## שלב 2 — להפעיל כניסה באימייל
1. בתפריט: **Authentication** → **Providers** → **Email** → ודא שהוא **Enabled**.
   (מספיק "Email"; אפשר לכבות "Confirm email" כדי שהקישור ייכנס ישר.)
2. **Authentication** → **URL Configuration**:
   - **Site URL**: הכתובת של האתר החי — `https://omerdd04.github.io/parkpals/`
   - **Redirect URLs**: הוסף גם את הכתובת הזו, וגם `http://localhost:5173/` (לפיתוח מקומי).

## שלב 3 — לחבר את המפתחות לאפליקציה
1. ב-Supabase: **Project Settings** → **API**. תמצא שם:
   - **Project URL**
   - **anon public** key
2. בפרויקט, צור קובץ בשם `.env` (העתק מ-`.env.example`) ומלא:
   ```
   VITE_SUPABASE_URL=https://xxxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGci...
   ```
   ⚠️ אלה המפתחות ה**ציבוריים** — מותר שהם באפליקציה (RLS מגן על הנתונים).
   **אל תשים** את ה-`service_role` key או את סיסמת בסיס הנתונים.

> אם אתה רוצה שאני אחבר אותם בשבילך: פשוט **הדבק לי כאן ב-chat את ה-Project URL
> ואת ה-anon public key** ואני מוסיף אותם ומעלה. (רק את השניים האלה — לא סודות.)

## שלב 4 — הפעלה
- מקומית: `npm install` ואז `npm run dev`.
- לאתר החי (GitHub Pages): צריך שהמפתחות יהיו זמינים בזמן ה-build. נגדיר אותם
  כ-**GitHub Secrets** ואטפל בזה כשנגיע לשם.

---

## מה כבר מחובר לשרת
- ✅ כניסה/הרשמה באימייל (קישור קסם)
- ✅ פרופיל בעלים+כלב נשמר בענן ומסתנכרן בין מכשירים

## מה מחכה לחיבור הבא (אחרי שהמפתחות עובדים)
- ⏳ נוכחות חיה בפארקים (מי שם עכשיו) — בזמן אמת
- ⏳ חברים אמיתיים לפי קוד אישי
- ⏳ צ'אט אמיתי בין משתמשים
- ⏳ תיבת תלונות שנשמרת בענן

**הצעד שלך עכשיו:** שלב 1–3. או פשוט תן לי כאן את ה-URL וה-anon key ואמשיך.
